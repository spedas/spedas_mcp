Always check dataset `Coverage` against your requested time range before fetching.
Use `browse_parameters(dataset_id)` to inspect variables before fetching.
`fetch_data` writes to disk and returns metadata + stats — data is NOT returned inline.