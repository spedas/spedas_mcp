{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.spedas/spedas-agent-kit",
  "title": "SPEDAS Agent Kit",
  "description": "Unified SPEDAS-oriented MCP server combining vendored CDAWeb, PDS PPI, and SPICE capabilities",
  "repository": {
    "url": "https://github.com/spedas/spedas_agent_kit",
    "source": "github"
  },
  "version": "0.1.0",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "spedas-agent-kit",
      "version": "0.1.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "XHELIO_CDAWEB_CACHE_DIR",
          "description": "Override the default CDAWeb cache directory (~/.cdawebmcp/)",
          "required": false
        },
        {
          "name": "PDSMCP_CACHE_DIR",
          "description": "Override the default PDS PPI cache directory (~/.pdsmcp/)",
          "required": false
        },
        {
          "name": "XHELIO_SPICE_KERNEL_DIR",
          "description": "Override the default SPICE kernel cache directory (~/.xhelio_spice/kernels/)",
          "required": false
        },
        {
          "name": "SPEDAS_AGENT_KIT_COMPAT_TOOLS",
          "description": "Set to 1 to advertise legacy CDAWeb/PDS compatibility browse/load/parameter/fetch tools; unset by default for the compact unified tool surface.",
          "required": false
        },
        {
          "name": "SPEDAS_AGENT_KIT_DATASOURCE_TOOLS",
          "description": "Set to 1 to advertise direct HAPI/FDSN data-source tools (browse_hapi_catalog, fetch_hapi_data, browse_fdsn_datasets, fetch_fdsn_data); unset by default because the compact unified discovery route remains preferred.",
          "required": false
        }
      ]
    }
  ]
}
