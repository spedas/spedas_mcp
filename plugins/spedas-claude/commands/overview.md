# /spedas-agent-kit:overview

Use the `spedas` MCP server to call `spedas_overview()`. Then explain, in a concise user-facing summary, which CDAWeb, PDS PPI, and SPICE capability groups are available and which first tool to use for the user's goal.

User goal: $ARGUMENTS
